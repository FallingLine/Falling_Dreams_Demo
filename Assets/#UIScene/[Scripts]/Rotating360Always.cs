using UnityEngine;

public class Rotating360Always : MonoBehaviour
{
    public Vector3 mainObject;
    public float speed = 1f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
